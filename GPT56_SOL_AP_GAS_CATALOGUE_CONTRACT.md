# 吉寶軒目錄資料契約：給 GPT-5.6 Sol 的無憑證版本

> 來源：`scripts/GAS/AntiqueAnalysis_AI.md` 的讀取型摘要。請不要把原始檔帶入外部 Chat：它目前含有明文服務憑證。
> 此文件只描述目前契約與待決設計；不授權 schema、GAS 或前端變更。

## 目前凍結的 Sheet 欄位

| # | 欄位 | GAS 輸出鍵 | 公開 API 現況 |
|---:|---|---|---|
| 0 | UUID | — | 不公開 |
| 1 | 入庫時間 | — | 不公開 |
| 2 | 用戶描述 | `userCaption` | 不公開 |
| 3 | 品名 | `itemName` | 公開 |
| 4 | 分類 | `category` | 公開 |
| 5 | 年代 | `era` | 公開；固定九值 enum |
| 6 | 故事 | `story` | 公開 |
| 7 | 拍賣參考品 | `refItem` | **目前未傳出** |
| 8 | 參考價格 | `refPrice` | 公開 |
| 9 | Drive URL | `imageUrl` | 公開（縮圖 URL） |
| 10 | 標籤 | `tags` | 公開 |
| 11 | 狀態 | `status` | 僅以「完成」篩選 |
| 12 | 展示建議 | `displayRecommendation` | 公開 |

## 生成、保存與公開之間的缺口

| 欄位 | Gemini 目前生成 | 寫入 Sheet | `doGet()` 傳出 | 前端已有用途 | 建議處置 |
|---|---|---|---|---|---|
| `features` | 是 | 否 | 否 | 無 | 先決定它是否為內部研究／人工覆核欄位；不要直接公開。
| `refItem` | 是 | 是 | 否 | modal 有版位 | P0：先把公開需求、真實性與措辭門檻定清楚，再做同步契約修正。
| `highlightQuote` | 是 | 否 | 否 | card／modal 有版位 | P1：若要公開，需 DD-XXX 決定儲存位置、字數與人工審核規則。
| `currentSellingPoint` | 是 | 否 | 否 | 無 | 不建議以「賣點」原樣公開；可改為內部 `collectorNote` 候選。
| `condition` | 註解中，未啟用 | 否 | 否 | 無 | 不可由照片斷言；改為照片可見狀態 + 實物未檢視標示。
| `provenance` | 註解中，未啟用 | 否 | 否 | 無 | 禁止圖片推測；應由人工文件證據狀態管理。

## 欄位設計提案：請 Sol 評估，不得直接啟用

### 第一批：不新增欄位的契約修復

1. 釐清 `refItem` 是否可公開；若可，需讓 `doGet()` 與 modal 一致，並保留「無直接可比拍品」的誠實 fallback。
2. 決定 `features` 是內部研究底稿、策展人覆核材料，或公開「器物觀察」；三者不能混用。
3. 決定 `highlightQuote` 是否值得保存；它應是圖錄式索引句，不可替代內容證據。

### 第二批：需 DD-XXX 的安全欄位

| 候選欄位 | 建議語意 | 公開性 | 不可接受的生成方式 |
|---|---|---|---|
| `conditionVisualNote` | 僅照片可見的狀態；必含「未經實物檢視」適用規則 | 需人工覆核 | AI 宣稱完整品相、修復史或真偽 |
| `provenanceEvidenceStatus` | `未提供`／`待查`／`文件待核`／`已核` | 可公開狀態，不公開文件細節 | 由圖像或風格猜測來源 |
| `provenanceEvidenceNote` | 已提供文件的中性描述與人工核對紀錄 | 預設內部 | 編造舊藏、傳承或來源鏈 |
| `researchStatus` | `影像初判`／`待實物覆核`／`資料補充中`／`完成` | 可作內部標籤 | 將 AI 影像初判包裝成鑑定結論 |
| `collectorNote` | 一句給收藏脈絡的中性提醒 | 公開前需審核 | 催購、投資保證、稀缺性炒作 |

## 不可破壞的限制

- `era` 只能使用既有九值：史前與高古、唐宋元(含之前)、明朝、清朝、民國、近現代、外國骨董、時代不詳、其他。
- 不得編造拍賣 Lot、成交價、買家、來源或鑑定結論。
- 欄位新增／改名／排序需同時修改 `writeToSheet()`、`doGet()`、`Publish/index.html`，並經 DD-XXX；本文件本身不是 DD。
